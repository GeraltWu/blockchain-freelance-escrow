# 默认使用同域 /api：本地由 Vite 代理，生产环境由 Nginx 代理。
# 只有 API 独立部署在其他域名时才改成完整 URL。
VITE_API_BASE_URL=/api

# FreelanceEscrow 合约部署地址(Sepolia);ABI 在 src/web3/abi/FreelanceEscrow.json
VITE_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000
